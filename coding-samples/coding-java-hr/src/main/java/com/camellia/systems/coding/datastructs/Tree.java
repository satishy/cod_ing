package com.camellia.systems.coding.datastructs;

/**
 * Created by satishdvla on 07/04/17.
 */
public class Tree {

    private Node root;

    public Node find(int id) {

        return new Node();
    }

    public void insert(int id, double fd) {

        Node newNode = new Node();

        newNode.getP1().setiData(id);
       //ewNode.getP1().setfData(fd);

        if(root == null) {
            root = newNode;
        } else {
            Node current = root;
            Node parent;
            while (true) {


            }


        }}

    public void delete(int id) {

    }

}
